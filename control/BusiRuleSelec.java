package gpw.control;

import java.util.ArrayList;
import java.time.LocalDate;
public interface BusiRuleSelec {

/*
public boolean validar(ArrayList x);
public boolean lancar(ArrayList y);
public boolean editar(ArrayList z);
public boolean confirmar(ArrayList w);
public boolean carregar(ArrayList v);
*/

	public boolean validar(ArrayList x);
	public boolean lancar(ArrayList y);
	public boolean editar(ArrayList z);
	public boolean confirmar(ArrayList w);
	public boolean carregar(ArrayList v);
}